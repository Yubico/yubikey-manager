[project]
name = "yubikey-manager"
version = "6.0.0-dev.0"
description = "Library and CLI for managing your YubiKey configuration."
authors = [
  { name = "Dain Nilsson", email = "dain@yubico.com" }
]
requires-python = ">=3.10, <4"
license = "BSD-2-Clause"
keywords = ["yubikey", "yubiotp", "piv", "fido"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: End Users/Desktop",
  "Topic :: Security :: Cryptography",
  "Topic :: Utilities"
]
dependencies = [
  "yubikit>=6.0.0.dev0",
  "cryptography>=2.1",
]

[project.urls]
Homepage = "https://github.com/Yubico/yubikey-manager"

[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[tool.maturin]
bindings = "bin"
manifest-path = "../../crates/ykman-cli/Cargo.toml"
python-packages = ["ykman"]
strip = true
