name: usbip-test

on: [push]

jobs:
  build:

    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v1

      - name: List kernel modules
        run: lsmod

      - name: Try usbip
        run: |
          usbip version
          usbip help
          usbip list --local
